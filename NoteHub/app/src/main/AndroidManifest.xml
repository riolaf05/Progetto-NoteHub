<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.unirc.pjam.notehub">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:logo="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="it.unirc.pjam.notehub.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="it.unirc.pjam.notehub.AppuntiActivity"
            android:label="@string/listaAppunti"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name="it.unirc.pjam.notehub.PostActivity"
            android:label="@string/listaPost"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
            android:parentActivityName="it.unirc.pjam.notehub.MainActivity">
        </activity>
        <activity
            android:name="it.unirc.pjam.notehub.DownloadActivity"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
            android:parentActivityName=".AppuntiActivity">

        </activity>
    </application>

</manifest>